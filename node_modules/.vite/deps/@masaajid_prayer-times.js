// node_modules/@masaajid/prayer-times/dist/index.js
var s = { ISNA: { fajr: 15, isha: 15, maghrib: "1 min", midnight: "Standard", adjustments: { fajr: -12.5, dhuhr: 5, asr: -1, maghrib: 2, isha: -1.5 } }, MWL: { fajr: 18, isha: 17, maghrib: "1 min", midnight: "Standard", adjustments: { dhuhr: 1 } }, Egypt: { fajr: 19.5, isha: 17.5, maghrib: "1 min", midnight: "Standard", adjustments: { fajr: -0.5, sunrise: -0.5, asr: 0.5, maghrib: -1 } }, UmmAlQura: { fajr: 18.5, isha: "90 min", maghrib: "1 min", midnight: "Standard" }, Qatar: { fajr: 18, isha: "90 min", maghrib: "1 min", midnight: "Standard", adjustments: { fajr: -0.5, maghrib: 2, isha: 3 } }, Dubai: { fajr: 18.2, isha: 18.2, maghrib: "1 min", midnight: "Standard", adjustments: { sunrise: -3.5, dhuhr: 3, asr: 1.5, maghrib: 2.5, isha: 0.5 } }, JAKIM: { fajr: 18, isha: 18, maghrib: "1 min", midnight: "Standard", adjustments: { fajr: 0.5, dhuhr: 1.5, maghrib: -0.5, asr: 0.5, isha: 0.5 } }, JAKIMKN: { fajr: 18, isha: 17, maghrib: "1 min", midnight: "Standard", adjustments: { fajr: 0.5, dhuhr: 1.5, maghrib: -0.5, asr: 0.5, isha: 0.5 } }, Kemenag: { fajr: 20, isha: 18, maghrib: "1 min", midnight: "Standard", adjustments: { fajr: 2, dhuhr: 3, asr: 2, maghrib: 2, isha: 2, sunrise: -4 } }, Singapore: { fajr: 20, isha: 18, maghrib: "1 min", midnight: "Standard", adjustments: { fajr: 0.5, sunrise: 0.5, dhuhr: 1, asr: 1, isha: 1 } }, France12: { fajr: 12, isha: 12, maghrib: "1 min", midnight: "Standard" }, France15: { fajr: 15, isha: 15, maghrib: "1 min", midnight: "Standard" }, France18: { fajr: 18, isha: 18, maghrib: "1 min", midnight: "Standard" }, Turkey: { fajr: 18, isha: 17, maghrib: "1 min", midnight: "Standard", adjustments: { sunrise: -7, dhuhr: 5, asr: 5.5, maghrib: 7, isha: 1.5 } }, Russia: { fajr: 16, isha: 15, maghrib: "1 min", midnight: "Standard", adjustments: { fajr: -0.5, sunrise: -0.5, dhuhr: -0.5, asr: 0.5, maghrib: -1.5, isha: -0.5 } }, Moonsighting: { fajr: 18, isha: 18, maghrib: "1 min", midnight: "Standard", shafaq: "general", adjustments: { dhuhr: 5, maghrib: 3 } }, Tehran: { fajr: 17.7, isha: 14, maghrib: 4.5, midnight: "Jafari" }, Jafari: { fajr: 16, isha: 14, maghrib: 4, midnight: "Jafari" }, Karachi: { fajr: 18, isha: 18, maghrib: "1 min", midnight: "Standard", adjustments: { dhuhr: 1 } }, Custom: { fajr: 18, isha: 17, maghrib: "1 min", midnight: "Standard" } };
var LX = { ISNA: "Islamic Society of North America", MWL: "Muslim World League", Egypt: "Egyptian General Authority of Survey", UmmAlQura: "Umm Al-Qura University, Saudi Arabia", Qatar: "Qatar", Dubai: "Dubai", JAKIM: "Jabatan Kemajuan Islam Malaysia", JAKIMKN: "Jabatan Kemajuan Islam Malaysia - Kelantan", Kemenag: "Kementerian Agama, Indonesia", Singapore: "Singapore", France12: "France (UOIF 12°)", France15: "France (Moderate 15°)", France18: "France (Grande Mosquée 18°)", Turkey: "Turkey Diyanet", Russia: "Spiritual Administration of Muslims in Russia", Moonsighting: "Moonsighting Committee Worldwide", Tehran: "Institute of Geophysics, University of Tehran", Jafari: "Jafari (Shia) Jurisprudence", Karachi: "University of Islamic Sciences, Karachi", Custom: "Custom Method" };
var SX = { ISNA: "Widely used in North America. Uses 15° for both Fajr and Isha.", MWL: "International standard used globally. Fajr at 18° and Isha at 17°.", Egypt: "Used in Egypt and surrounding regions. Higher angles for twilight prayers.", UmmAlQura: "Official method of Saudi Arabia. Uses 90-minute interval for Isha.", Qatar: "Official method used in Qatar.", Dubai: "Method used in Dubai and UAE.", JAKIM: "Official method of Malaysia. Uses higher angles for tropical regions.", JAKIMKN: "Official method of Malaysia - Kelantan variant. Uses 17° for Isha prayer.", Kemenag: "Official method of Indonesia.", Singapore: "Official method used in Singapore.", France12: "UOIF method. Uses 12° for both Fajr and Isha.", France15: "Moderate French method. Uses 15° for both Fajr and Isha.", France18: "Grande Mosquée de Paris method. Uses 18° for both Fajr and Isha.", Turkey: "Official method used in Turkey.", Russia: "Adapted for very high latitudes in Russia.", Moonsighting: "Based on actual moon sighting traditions. Uses seasonal adjustments.", Tehran: "Shia method used in Iran. Different maghrib calculation.", Jafari: "Traditional Shia jurisprudence method.", Karachi: "Generally applicable method from University of Islamic Sciences, Karachi. Uses standard 18° angles.", Custom: "User-defined custom calculation parameters." };
function r(X) {
  let Z = s[X];
  if (!Z) throw Error(`Unknown calculation method: ${X}`);
  return { ...Z };
}
function RZ(X) {
  return LX[X] ?? X;
}
function DZ(X) {
  return SX[X] ?? "No description available";
}
function QX(X) {
  return X in s;
}
function OZ() {
  return Object.keys(s);
}
function KX(X) {
  if (typeof X === "number") return { type: "angle", value: X };
  let Z = /^(\d+(?:\.\d+)?)\s*min$/i.exec(X);
  if (Z) return { type: "interval", value: parseFloat(Z[1]) };
  let $ = parseFloat(X);
  if (!isNaN($)) return { type: "angle", value: $ };
  throw Error(`Invalid angle or interval format: ${X}`);
}
var CX = Math.PI;
var L0 = 2 * Math.PI;
var F = (X) => X * CX / 180;
var z = (X) => X * 180 / CX;
var I = (X) => Math.sin(F(X));
var w = (X) => Math.cos(F(X));
var WX = (X) => Math.tan(F(X));
var T = (X) => z(Math.asin(X));
var vX = (X) => z(Math.acos(X));
var kX = (X, Z) => z(Math.atan2(X, Z));
function N(X) {
  return X - 360 * Math.floor(X / 360);
}
function GX(X, Z) {
  return X - Z * Math.floor(X / Z);
}
function bX(X) {
  if (X >= -180 && X <= 180) return X;
  return X - 360 * Math.round(X / 360);
}
var b = F;
var jX = z;
var pX = N;
var o = { latitude: 21.422487, longitude: 39.826206 };
var MZ = 6371;
function PZ(X) {
  return typeof X === "number" && X >= -90 && X <= 90 && !isNaN(X);
}
function xZ(X) {
  return typeof X === "number" && X >= -180 && X <= 180 && !isNaN(X);
}
function A(X) {
  let Z, $;
  if (Array.isArray(X)) {
    if (X.length !== 2) throw Error("Coordinates array must contain exactly 2 elements [latitude, longitude]");
    [Z, $] = X;
  } else if (typeof X === "object" && X !== null) Z = X.latitude, $ = X.longitude;
  else throw Error("Coordinates must be an array [lat, lng] or object {latitude, longitude}");
  if (!PZ(Z)) throw Error(`Invalid latitude: ${Z}. Must be between -90 and 90 degrees`);
  if (!xZ($)) throw Error(`Invalid longitude: ${$}. Must be between -180 and 180 degrees`);
  return { latitude: Z, longitude: $ };
}
function P(X) {
  if (typeof X === "string") return AZ(X);
  return A(X);
}
function AZ(X) {
  let Z = X.trim().replace(/\s+/g, " "), $ = [/^(-?\d+\.?\d*),\s*(-?\d+\.?\d*)$/, /^(\d+\.?\d*)\s*([NS]),\s*(\d+\.?\d*)\s*([EW])$/i, /^(\d+)°(\d+)'(\d+)"?\s*([NS]),\s*(\d+)°(\d+)'(\d+)"?\s*([EW])$/i, /^(\d+)°(\d+\.?\d*)'?\s*([NS]),\s*(\d+)°(\d+\.?\d*)'?\s*([EW])$/i], J = $[0].exec(Z);
  if (J) return A([parseFloat(J[1]), parseFloat(J[2])]);
  let K = $[1].exec(Z);
  if (K) {
    let k = parseFloat(K[1]), G = parseFloat(K[3]);
    if (K[2].toUpperCase() === "S") k = -k;
    if (K[4].toUpperCase() === "W") G = -G;
    return A([k, G]);
  }
  let Q = $[2].exec(Z);
  if (Q) {
    let k = parseInt(Q[1]) + parseInt(Q[2]) / 60 + parseInt(Q[3]) / 3600, G = parseInt(Q[5]) + parseInt(Q[6]) / 60 + parseInt(Q[7]) / 3600, B = Q[4].toUpperCase() === "S" ? -k : k, _ = Q[8].toUpperCase() === "W" ? -G : G;
    return A([B, _]);
  }
  let W = $[3].exec(Z);
  if (W) {
    let k = parseInt(W[1]) + parseFloat(W[2]) / 60, G = parseInt(W[4]) + parseFloat(W[5]) / 60, B = W[3].toUpperCase() === "S" ? -k : k, _ = W[6].toUpperCase() === "W" ? -G : G;
    return A([B, _]);
  }
  throw Error(`Unable to parse coordinates: ${X}`);
}
function LZ(X, Z) {
  let $ = F(X.latitude), J = F(Z.latitude), K = F(Z.latitude - X.latitude), Q = F(Z.longitude - X.longitude), W = Math.sin(K / 2) * Math.sin(K / 2) + Math.cos($) * Math.cos(J) * Math.sin(Q / 2) * Math.sin(Q / 2), k = 2 * Math.atan2(Math.sqrt(W), Math.sqrt(1 - W));
  return MZ * k;
}
function SZ(X) {
  return LZ(X, o);
}
function CZ(X) {
  let Z = F(X.latitude), $ = F(o.latitude), J = F(o.longitude - X.longitude), K = Math.sin(J) * Math.cos($), Q = Math.cos(Z) * Math.sin($) - Math.sin(Z) * Math.cos($) * Math.cos(J);
  return (z(Math.atan2(K, Q)) + 360) % 360;
}
function BX(X) {
  let { latitude: Z, longitude: $ } = X;
  if (Z >= 25 && Z <= 72 && $ >= -168 && $ <= -52) return "north_america";
  if (Z >= 35 && Z <= 72 && $ >= -10 && $ <= 40) return "europe";
  if (Z >= 12 && Z <= 42 && $ >= 34 && $ <= 60) return "middle_east";
  if (Z >= -11 && Z <= 28 && $ >= 95 && $ <= 141) return "southeast_asia";
  if (Z >= -35 && Z <= 37 && $ >= -18 && $ <= 51) return "africa";
  if (Z >= 5 && Z <= 37 && $ >= 60 && $ <= 97) return "south_asia";
  return "unknown";
}
function yX(X, Z) {
  let { latitude: $, longitude: J } = X;
  return $ >= Z.south && $ <= Z.north && J >= Z.west && J <= Z.east;
}
var vZ = SZ;
var bZ = CZ;
function L(X) {
  let Z = [];
  if (!X.method) Z.push({ field: "method", message: "Calculation method is required", code: "INVALID_METHOD" });
  else if (!QX(X.method)) Z.push({ field: "method", message: `Invalid calculation method: ${String(X.method)}`, code: "INVALID_METHOD" });
  if (!X.location) Z.push({ field: "location", message: "Location coordinates are required", code: "INVALID_COORDINATES" });
  else try {
    if (Array.isArray(X.location)) A(X.location);
    else A([X.location.latitude, X.location.longitude]);
  } catch ($) {
    Z.push({ field: "location", message: $ instanceof Error ? $.message : "Invalid coordinates", code: "INVALID_COORDINATES" });
  }
  if (X.timezone !== void 0) {
    let $ = jZ(X.timezone);
    if (!$.isValid) Z.push({ field: "timezone", message: $.error, code: "INVALID_TIMEZONE" });
  }
  if (X.date !== void 0) {
    let $ = pZ(X.date);
    if (!$.isValid) Z.push({ field: "date", message: $.error, code: "INVALID_DATE" });
  }
  if (X.asrSchool !== void 0) {
    let $ = yZ(X.asrSchool);
    if (!$.isValid) Z.push({ field: "asrSchool", message: $.error, code: "VALIDATION_ERROR" });
  }
  if (X.highLatitudeRule !== void 0) {
    let $ = fZ(X.highLatitudeRule);
    if (!$.isValid) Z.push({ field: "highLatitudeRule", message: $.error, code: "VALIDATION_ERROR" });
  }
  if (X.elevation !== void 0) {
    let $ = TZ(X.elevation);
    if (!$.isValid) Z.push({ field: "elevation", message: $.error, code: "VALIDATION_ERROR" });
  }
  if (X.adjustments !== void 0) {
    let $ = hZ(X.adjustments);
    if (!$.isValid) Z.push({ field: "adjustments", message: $.error, code: "INVALID_ADJUSTMENT" });
  }
  if (X.format !== void 0) {
    let $ = gZ(X.format);
    if (!$.isValid) Z.push({ field: "format", message: $.error, code: "VALIDATION_ERROR" });
  }
  if (X.rounding !== void 0) {
    let $ = mZ(X.rounding);
    if (!$.isValid) Z.push({ field: "rounding", message: $.error, code: "VALIDATION_ERROR" });
  }
  if (X.iterations !== void 0) {
    let $ = uZ(X.iterations);
    if (!$.isValid) Z.push({ field: "iterations", message: $.error, code: "VALIDATION_ERROR" });
  }
  return { isValid: Z.length === 0, errors: Z };
}
function jZ(X) {
  if (typeof X !== "string") return { isValid: false, error: "Timezone must be a string" };
  if (X.trim().length === 0) return { isValid: false, error: "Timezone cannot be empty" };
  if (!/^[A-Za-z_]+\/[A-Za-z_]+$/.test(X) && X !== "UTC") return { isValid: false, error: 'Timezone must be in format "Region/City" (e.g., "America/New_York") or "UTC"' };
  return { isValid: true };
}
function pZ(X) {
  if (!(X instanceof Date)) return { isValid: false, error: "Date must be a Date object" };
  if (isNaN(X.getTime())) return { isValid: false, error: "Invalid date provided" };
  let Z = X.getFullYear();
  if (Z < 1900 || Z > 2200) return { isValid: false, error: "Date must be between years 1900 and 2200" };
  return { isValid: true };
}
function yZ(X) {
  let Z = ["Standard", "Hanafi"];
  if (!Z.includes(X)) return { isValid: false, error: `Asr school must be one of: ${Z.join(", ")}` };
  return { isValid: true };
}
function fZ(X) {
  let Z = ["NightMiddle", "AngleBased", "OneSeventh", "None"];
  if (!Z.includes(X)) return { isValid: false, error: `High latitude rule must be one of: ${Z.join(", ")}` };
  return { isValid: true };
}
function TZ(X) {
  if (typeof X !== "number" || isNaN(X)) return { isValid: false, error: "Elevation must be a number" };
  if (X < -500 || X > 1e4) return { isValid: false, error: "Elevation must be between -500 and 10,000 meters" };
  return { isValid: true };
}
function hZ(X) {
  if (typeof X !== "object" || X === null) return { isValid: false, error: "Adjustments must be an object" };
  let Z = ["fajr", "sunrise", "dhuhr", "asr", "maghrib", "isha"];
  for (let [$, J] of Object.entries(X)) {
    if (!Z.includes($)) return { isValid: false, error: `Invalid prayer name in adjustments: ${$}. Valid names are: ${Z.join(", ")}` };
    if (typeof J !== "number" || isNaN(J)) return { isValid: false, error: `Adjustment for ${$} must be a number` };
    if (Math.abs(J) > 60) return { isValid: false, error: `Adjustment for ${$} must be between -60 and +60 minutes` };
  }
  return { isValid: true };
}
function gZ(X) {
  let Z = ["24h", "12h", "12hNS", "timestamp", "iso8601"];
  if (!Z.includes(X)) return { isValid: false, error: `Time format must be one of: ${Z.join(", ")}` };
  return { isValid: true };
}
function mZ(X) {
  let Z = ["nearest", "up", "down", "none"];
  if (!Z.includes(X)) return { isValid: false, error: `Rounding method must be one of: ${Z.join(", ")}` };
  return { isValid: true };
}
function uZ(X) {
  if (typeof X !== "number" || isNaN(X)) return { isValid: false, error: "Iterations must be a number" };
  if (!Number.isInteger(X) || X < 1 || X > 10) return { isValid: false, error: "Iterations must be an integer between 1 and 10" };
  return { isValid: true };
}
function fX(X) {
  let Z = { ...X };
  if (Z.location) try {
    let $ = P(Z.location);
    Z.location = [$.latitude, $.longitude];
  } catch {
  }
  if (Z.timezone && typeof Z.timezone === "string") Z.timezone = Z.timezone.trim();
  if (Z.elevation !== void 0 && typeof Z.elevation === "string") {
    let $ = parseFloat(Z.elevation);
    if (!isNaN($)) Z.elevation = $;
  }
  if (Z.iterations !== void 0) Z.iterations = Math.round(Z.iterations);
  return Z;
}
function h(X, Z, $, J = 0) {
  let K = Math.trunc, Q = K(Z > 2 ? X : X - 1), W = K(Z > 2 ? Z : Z + 12), k = $ + J / 24, G = K(Q / 100), B = K(2 - G + K(G / 4)), _ = K(365.25 * (Q + 4716)), Y = K(30.6001 * (W + 1));
  return _ + Y + k + B - 1524.5;
}
function n(X) {
  return (X - 2451545) / 36525;
}
function g(X) {
  let Z = X, $ = 280.4664567, J = 36000.76983 * Z, K = 3032e-7 * Math.pow(Z, 2), Q = 280.4664567 + J + K;
  return N(Q);
}
function hX(X) {
  let Z = X, $ = 218.3165, K = 218.3165 + 481267.8813 * Z;
  return N(K);
}
function gX(X) {
  let Z = X, $ = 125.04452, J = 1934.136261 * Z, K = 20708e-7 * Math.pow(Z, 2), Q = Math.pow(Z, 3) / 45e4, W = 125.04452 - J + K + Q;
  return N(W);
}
function i(X) {
  let Z = X, $ = 357.52911, J = 35999.05029 * Z, K = 1537e-7 * Math.pow(Z, 2), Q = 357.52911 + J - K;
  return N(Q);
}
function _X(X, Z) {
  let $ = X, J = F(Z), K = (1.914602 - 4817e-6 * $ - 14e-6 * Math.pow($, 2)) * Math.sin(J), Q = (0.019993 - 101e-6 * $) * Math.sin(2 * J), W = 289e-6 * Math.sin(3 * J);
  return K + Q + W;
}
function mX(X, Z) {
  let $ = X, K = Z + _X($, i($)), Q = 125.04 - 1934.136 * $, W = K - 569e-5 - 478e-5 * I(Q);
  return N(W);
}
function m(X) {
  let Z = X, $ = 23.439291, J = 0.013004167 * Z, K = 1639e-10 * Math.pow(Z, 2), Q = 5036e-10 * Math.pow(Z, 3);
  return 23.439291 - J - K + Q;
}
function t(X, Z) {
  let $ = X, J = Z, K = 125.04 - 1934.136 * $;
  return J + 256e-5 * w(K);
}
function UX(X) {
  let Z = X, $ = Z * 36525 + 2451545, J = 280.46061837, K = 360.98564736629 * ($ - 2451545), Q = 387933e-9 * Math.pow(Z, 2), W = Math.pow(Z, 3) / 3871e4, k = 280.46061837 + K + Q - W;
  return N(k);
}
function uX(X, Z, $, J) {
  let K = Z, Q = $, W = J, k = -0.0047777777777777775 * I(W), G = 36666666666666667e-20 * I(2 * K), B = 638888888888889e-19 * I(2 * Q), _ = 5833333333333333e-20 * I(2 * W);
  return k - G - B + _;
}
function cX(X, Z, $, J) {
  let K = Z, Q = $, W = J, k = 0.0025555555555555553 * w(W), G = 15833333333333332e-20 * w(2 * K), B = 2777777777777778e-20 * w(2 * Q), _ = 24999999999999998e-21 * w(2 * W);
  return k + G + B - _;
}
function lX(X, Z, $) {
  let J = X, K = Z, Q = $, W = I(J) * I(K), k = w(J) * w(K) * w(Q);
  return T(W + k);
}
function dX(X, Z, $, J) {
  let K = X - Z, Q = $ - X, W = Q - K;
  return X + J / 2 * (K + Q + J * W);
}
function YX(X, Z, $, J) {
  let K = N(X - Z), Q = N($ - X), W = Q - K;
  return X + J / 2 * (K + Q + J * W);
}
function sX(X, Z) {
  return X + Z;
}
function rX(X, Z) {
  let J = 125.04 - 1934.136 * Z;
  return X - 569e-5 - 478e-5 * I(J);
}
function oX(X) {
  let Z = X, $ = g(Z), J = m(Z), K = i(Z), Q = Math.pow(WX(J / 2), 2), W = I(2 * $), k = I(K), G = w(2 * $), B = I(4 * $), _ = I(2 * K);
  return (Q * W - 0.0334 * k + 0.0668 * Q * k * G - 0.5 * Q * Q * B - 34861249999999994e-20 * _) * 4 / 60;
}
function TX(X) {
  if (X % 4 !== 0) return false;
  if (X % 100 === 0 && X % 400 !== 0) return false;
  return true;
}
function y(X, Z, $) {
  let J = 0, K = 10, Q = TX(Z) ? 173 : 172, W = TX(Z) ? 366 : 365;
  if ($ >= 0) {
    if (J = X + K, J >= W) J = J - W;
  } else if (J = X - Q, J < 0) J = J + W;
  return J;
}
function u(X) {
  let Z = new Date(X.getFullYear(), 0, 0), $ = X.getTime() - Z.getTime();
  return Math.floor($ / 864e5);
}
function cZ(X, Z = "24h", $) {
  if (isNaN(X)) return "-----";
  if (Z === "timestamp") return Math.floor(X).toString();
  let J = new Date(X);
  if (Z === "iso8601") return $ ? J.toISOString() : J.toISOString();
  let K = { hour: Z === "24h" ? "2-digit" : "numeric", minute: "2-digit", hour12: Z !== "24h" };
  if ($) K.timeZone = $;
  let Q = J.toLocaleTimeString("en-US", K);
  if (Z === "12hNS") Q = Q.replace(/ ?[AP]M/, "");
  return Q;
}
function E(X, Z) {
  return new Date(X.getTime() + Z * 60 * 1e3);
}
function S(X, Z) {
  return new Date(X.getTime() + Z * 1e3);
}
var D = { Nearest: "nearest", Up: "up", None: "none" };
function C(X, Z = D.Nearest) {
  let $ = X.getUTCSeconds(), J = 0;
  if (Z === D.Nearest) J = $ >= 30 ? 60 - $ : -1 * $;
  else if (Z === D.Up) J = $ > 0 ? 60 - $ : 0;
  else if (Z === D.None) J = 0;
  return S(X, J);
}
function v(X, Z) {
  try {
    return X;
  } catch ($) {
    return console.warn(`Timezone conversion failed for ${Z}:`, $), new Date(X);
  }
}
function qX(X, Z = "24h", $) {
  let J = {};
  for (let [K, Q] of Object.entries(X)) if (Q instanceof Date) J[K] = cZ(Q.getTime(), Z);
  return J;
}
function a(X, Z = 48) {
  return Math.abs(X) >= Z;
}
function lZ(X, Z, $) {
  switch ($) {
    case "NightMiddle":
      return X * 0.5;
    case "OneSeventh":
      return X / 7;
    case "AngleBased":
      return X * (Z / 60);
    default:
      return X * 0.5;
  }
}
function nX(X, Z, $) {
  let J = lZ(Z.night, Z.angle, $), K = Z.direction ?? 1, Q = J * K * 3600;
  return S(X, Q);
}
function dZ(X) {
  let { latitude: Z, dayOfYear: $, year: J, baseTime: K } = X, Q = 75 + 0.5209090909090909 * Math.abs(Z), W = 75 + 0.35345454545454547 * Math.abs(Z), k = 75 + 0.5952727272727273 * Math.abs(Z), G = 75 + 0.8745454545454546 * Math.abs(Z), B = y($, J, Z), _;
  if (B < 91) _ = Q + (W - Q) / 91 * B;
  else if (B < 137) _ = W + (k - W) / 46 * (B - 91);
  else if (B < 183) _ = k + (G - k) / 46 * (B - 137);
  else if (B < 229) _ = G + (k - G) / 46 * (B - 183);
  else if (B < 275) _ = k + (W - k) / 46 * (B - 229);
  else _ = W + (Q - W) / 91 * (B - 275);
  return S(K, Math.round(_ * -60));
}
function sZ(X, Z = "general") {
  let { latitude: $, dayOfYear: J, year: K, baseTime: Q } = X, W, k, G, B;
  if (Z === "ahmer") W = 62 + 0.31636363636363635 * Math.abs($), k = 62 - 0.13018181818181818 * Math.abs($), G = 62 + 0.0930909090909091 * Math.abs($), B = 62 + 0.35345454545454547 * Math.abs($);
  else if (Z === "abyad") W = 75 + 0.46545454545454545 * Math.abs($), k = 75 + 0.13018181818181818 * Math.abs($), G = 75 + 0.6698181818181819 * Math.abs($), B = 75 + 1.488 * Math.abs($);
  else W = 75 + 0.46545454545454545 * Math.abs($), k = 75 + 0.03727272727272727 * Math.abs($), G = 75 - 0.16745454545454547 * Math.abs($), B = 75 + 0.11163636363636363 * Math.abs($);
  let _ = y(J, K, $), Y;
  if (_ < 91) Y = W + (k - W) / 91 * _;
  else if (_ < 137) Y = k + (G - k) / 46 * (_ - 91);
  else if (_ < 183) Y = G + (B - G) / 46 * (_ - 137);
  else if (_ < 229) Y = B + (G - B) / 46 * (_ - 183);
  else if (_ < 275) Y = G + (k - G) / 46 * (_ - 229);
  else Y = k + (W - k) / 91 * (_ - 275);
  return S(Q, Math.round(Y * 60));
}
function iX(X, Z, $) {
  if ($) return ($.getTime() - Z.getTime()) / 36e5;
  let J = new Date(X);
  return J.setDate(J.getDate() + 1), iX(X, Z, J);
}
function tX(X, Z, $, J, K = "NightMiddle") {
  let Q = iX(X.sunrise, X.sunset), W = u($), k = X.fajr;
  if (!k || isNaN(k.getTime())) if (a(Z.latitude) && K !== "None") k = nX(X.sunrise, { latitude: Z.latitude, angle: J.fajr, night: Q, direction: -1 }, K);
  else k = dZ({ latitude: Z.latitude, dayOfYear: W, year: $.getFullYear(), baseTime: X.sunrise });
  let G = X.isha;
  if (!G || isNaN(G.getTime())) if (a(Z.latitude) && K !== "None" && J.isha) G = nX(X.sunset, { latitude: Z.latitude, angle: J.isha, night: Q, direction: 1 }, K);
  else G = sZ({ latitude: Z.latitude, dayOfYear: W, year: $.getFullYear(), baseTime: X.sunset });
  let B = X.maghrib ?? X.sunset;
  return { fajr: k ?? X.sunrise, isha: G ?? X.sunset, maghrib: B };
}
var f = class {
  declination;
  rightAscension;
  apparentSiderealTime;
  constructor(X) {
    let Z = n(X), $ = g(Z), J = hX(Z), K = gX(Z), Q = F(mX(Z, $)), W = UX(Z), k = uX(Z, $, J, K), G = cX(Z, $, J, K), B = m(Z), _ = F(t(Z, B));
    this.declination = z(Math.asin(Math.sin(_) * Math.sin(Q))), this.rightAscension = N(z(Math.atan2(Math.cos(_) * Math.sin(Q), Math.cos(Q)))), this.apparentSiderealTime = W + k * 3600 * Math.cos(F(B + G)) / 3600;
  }
};
function aX(X, Z, $) {
  let J = X, K = Z, Q = $, W = J * -1;
  return GX((Q + W - K) / 360, 1);
}
function eX(X, Z, $, J, K, Q) {
  let W = X, k = Z, G = $, B = J, _ = K, Y = Q, q = k * -1, H = N(G + 360.985647 * W), U = N(YX(B, _, Y, W)), V = bX(H - q - U) / -360;
  return (W + V) * 24;
}
function e(X, Z, $, J, K, Q, W, k, G, B, _) {
  let Y = X, q = Z, H = K, U = Q, M = W, V = k, x = G, JX = B, qZ = _, FZ = $.longitude * -1, VZ = Math.sin(F(q)) - Math.sin(F($.latitude)) * Math.sin(F(x)), IZ = Math.cos(F($.latitude)) * Math.cos(F(x)), PX = z(Math.acos(VZ / IZ)), d = J ? Y + PX / 360 : Y - PX / 360, NZ = N(H + 360.985647 * d), HZ = N(YX(U, M, V, d)), xX = dX(x, JX, qZ, d), AX = NZ - FZ - HZ, wZ = lX($.latitude, xX, AX) - q, EZ = 360 * Math.cos(F(xX)) * Math.cos(F($.latitude)) * Math.sin(F(AX)), zZ = wZ / EZ;
  return (d + zZ) * 24;
}
function XZ(X, Z, $, J) {
  let K = 75 + 0.5209090909090909 * Math.abs(X), Q = 75 + 0.35345454545454547 * Math.abs(X), W = 75 + 0.5952727272727273 * Math.abs(X), k = 75 + 0.8745454545454546 * Math.abs(X), G = y(Z, $, X), B;
  if (G < 91) B = K + (Q - K) / 91 * G;
  else if (G < 137) B = Q + (W - Q) / 46 * (G - 91);
  else if (G < 183) B = W + (k - W) / 46 * (G - 137);
  else if (G < 229) B = k + (W - k) / 46 * (G - 183);
  else if (G < 275) B = W + (Q - W) / 46 * (G - 229);
  else B = Q + (K - Q) / 91 * (G - 275);
  return S(J, Math.round(B * -60));
}
function ZZ(X, Z, $, J, K = "general") {
  let Q, W, k, G;
  if (K === "ahmer") Q = 62 + 0.31636363636363635 * Math.abs(X), W = 62 - 0.13018181818181818 * Math.abs(X), k = 62 + 0.0930909090909091 * Math.abs(X), G = 62 + 0.35345454545454547 * Math.abs(X);
  else if (K === "abyad") Q = 75 + 0.46545454545454545 * Math.abs(X), W = 75 + 0.13018181818181818 * Math.abs(X), k = 75 + 0.6698181818181819 * Math.abs(X), G = 75 + 1.488 * Math.abs(X);
  else Q = 75 + 0.46545454545454545 * Math.abs(X), W = 75 + 0.13018181818181818 * Math.abs(X), k = 75 + 0.6698181818181819 * Math.abs(X), G = 75 + 1.488 * Math.abs(X);
  let B = y(Z, $, X), _;
  if (B < 91) _ = Q + (W - Q) / 91 * B;
  else if (B < 137) _ = W + (k - W) / 46 * (B - 91);
  else if (B < 183) _ = k + (G - k) / 46 * (B - 137);
  else if (B < 229) _ = G + (k - G) / 46 * (B - 183);
  else if (B < 275) _ = k + (W - k) / 46 * (B - 229);
  else _ = W + (Q - W) / 91 * (B - 275);
  return S(J, Math.round(_ * 60));
}
var j = class {
  observer;
  solar;
  prevSolar;
  nextSolar;
  approxTransit;
  transit;
  sunrise;
  sunset;
  constructor(X, Z) {
    let $ = h(X.getFullYear(), X.getMonth() + 1, X.getDate(), 0);
    this.observer = Z, this.solar = new f($), this.prevSolar = new f($ - 1), this.nextSolar = new f($ + 1);
    let J = aX(Z.longitude, this.solar.apparentSiderealTime, this.solar.rightAscension), K = -0.8333333333333334;
    this.approxTransit = J, this.transit = eX(J, Z.longitude, this.solar.apparentSiderealTime, this.solar.rightAscension, this.prevSolar.rightAscension, this.nextSolar.rightAscension), this.sunrise = e(J, K, Z, false, this.solar.apparentSiderealTime, this.solar.rightAscension, this.prevSolar.rightAscension, this.nextSolar.rightAscension, this.solar.declination, this.prevSolar.declination, this.nextSolar.declination), this.sunset = e(J, K, Z, true, this.solar.apparentSiderealTime, this.solar.rightAscension, this.prevSolar.rightAscension, this.nextSolar.rightAscension, this.solar.declination, this.prevSolar.declination, this.nextSolar.declination);
  }
  hourAngle(X, Z) {
    return e(this.approxTransit, X, this.observer, Z, this.solar.apparentSiderealTime, this.solar.rightAscension, this.prevSolar.rightAscension, this.nextSolar.rightAscension, this.solar.declination, this.prevSolar.declination, this.nextSolar.declination);
  }
  afternoon(X) {
    let Z = Math.abs(this.observer.latitude - this.solar.declination), $ = X + Math.tan(F(Z)), J = z(Math.atan(1 / $));
    return this.hourAngle(J, true);
  }
};
var rZ = { maxIterations: 3, convergenceThreshold: 30, enableHighLatitudeAdjustment: true, elevationCorrection: true };
function FX(X) {
  let { date: Z, coordinates: $, methodParams: J } = X, K = new j(Z, $), Q = {}, W = 0, k = 0, G = false;
  Q.sunrise = O(K.sunrise, Z), Q.sunset = O(K.sunset, Z);
  let B = oZ(K, J, X, Q.sunrise);
  Q.fajr = O(B, Z), Q.dhuhr = O(K.transit, Z);
  let _ = X.asrSchool === "Hanafi" ? 2 : 1, Y = K.afternoon(_);
  Q.asr = O(Y, Z);
  let q = iZ(K, J, X);
  Q.maghrib = O(q, Z);
  let H = tZ(K, J, X, Q.sunset);
  Q.isha = O(H, Z), W = 1, k = 0, G = true;
  let U = { fajr: Q.fajr, sunrise: Q.sunrise, dhuhr: Q.dhuhr, asr: Q.asr, sunset: Q.sunset, maghrib: Q.maghrib, isha: Q.isha };
  if (J.adjustments) {
    let V = J.adjustments;
    U = { fajr: V.fajr ? E(U.fajr, V.fajr) : U.fajr, sunrise: V.sunrise ? E(U.sunrise, V.sunrise) : U.sunrise, dhuhr: V.dhuhr ? E(U.dhuhr, V.dhuhr) : U.dhuhr, asr: V.asr ? E(U.asr, V.asr) : U.asr, sunset: U.sunset, maghrib: V.maghrib ? E(U.maghrib, V.maghrib) : U.maghrib, isha: V.isha ? E(U.isha, V.isha) : U.isha };
  }
  return { value: { fajr: C(U.fajr, D.Nearest), sunrise: C(U.sunrise, D.Nearest), dhuhr: C(U.dhuhr, D.Nearest), asr: C(U.asr, D.Nearest), sunset: C(U.sunset, D.Nearest), maghrib: C(U.maghrib, D.Nearest), isha: C(U.isha, D.Nearest) }, iterations: W, converged: G, error: k, reason: G ? "Calculation converged successfully" : "Maximum iterations reached" };
}
function O(X, Z) {
  let $ = Z.getUTCFullYear(), J = Z.getUTCMonth(), K = Z.getUTCDate(), Q = X, W = K;
  while (Q < 0) Q += 24, W -= 1;
  while (Q >= 24) Q -= 24, W += 1;
  let k = Math.floor(Q), G = Math.floor(Q % 1 * 60), B = Math.floor(Q % 1 * 60 % 1 * 60);
  return new Date(Date.UTC($, J, W, k, G, B));
}
function oZ(X, Z, $, J) {
  let K = X.hourAngle(-Z.fajr, false);
  if ($.methodCode === "Moonsighting" && Math.abs($.coordinates.latitude) >= 55) {
    let W = new Date($.date);
    W.setDate(W.getDate() + 1);
    let k = new j(W, $.coordinates), G = O(k.sunrise, W), B = O(X.sunset, $.date), Y = (Number(G) - Number(B)) / 1e3 / 7;
    K = (new Date(J.getTime() - Y * 1e3).getTime() - $.date.getTime()) / 36e5;
  }
  let Q = nZ($, J);
  if (isNaN(K) || Q !== null && (Q.getTime() - $.date.getTime()) / 36e5 > K) return Q !== null ? (Q.getTime() - $.date.getTime()) / 36e5 : K;
  return K;
}
function nZ(X, Z) {
  if (X.methodCode === "Moonsighting") {
    let $ = u(X.date);
    return XZ(X.coordinates.latitude, $, X.date.getFullYear(), Z);
  }
  return null;
}
function iZ(X, Z, $) {
  if (!Z.maghrib) return X.sunset;
  let J = KX(Z.maghrib);
  if (J.type === "angle") return X.hourAngle(-J.value, true);
  else return X.sunset + J.value / 60;
}
function tZ(X, Z, $, J) {
  let K = KX(Z.isha), Q;
  if (K.type === "angle") Q = X.hourAngle(-K.value, true);
  else Q = X.sunset + K.value / 60;
  if ($.methodCode === "Moonsighting" && Math.abs($.coordinates.latitude) >= 55) {
    let k = new Date($.date);
    k.setDate(k.getDate() + 1);
    let G = new j(k, $.coordinates), B = O(G.sunrise, k), _ = O(X.sunset, $.date), q = (Number(B) - Number(_)) / 1e3 / 7;
    Q = (new Date(J.getTime() + q * 1e3).getTime() - $.date.getTime()) / 36e5;
  }
  let W = aZ($, J);
  if (isNaN(Q) || W !== null && (W.getTime() - $.date.getTime()) / 36e5 < Q) return W !== null ? (W.getTime() - $.date.getTime()) / 36e5 : Q;
  return Q;
}
function aZ(X, Z) {
  if (X.methodCode === "Moonsighting") {
    let $ = u(X.date), J = X.methodParams.shafaq === "ahmer" ? "ahmer" : X.methodParams.shafaq === "abyad" ? "abyad" : "general";
    return ZZ(X.coordinates.latitude, $, X.date.getFullYear(), Z, J);
  }
  return null;
}
function eZ(X, Z) {
  let $ = [], { coordinates: J } = Z;
  if (X.fajr >= X.sunrise) $.push("Fajr time is not before sunrise");
  if (X.sunrise >= X.dhuhr) $.push("Sunrise time is not before Dhuhr");
  if (X.dhuhr >= X.asr) $.push("Dhuhr time is not before Asr");
  if (X.asr >= X.maghrib) $.push("Asr time is not before Maghrib");
  if (X.maghrib >= X.isha) $.push("Maghrib time is not before Isha");
  let K = Math.abs(J.latitude), Q = K >= 48, W = K >= 60, k = W ? 300 : Q ? 240 : 180, G = W ? 360 : Q ? 300 : 240, B = (X.sunrise.getTime() - X.fajr.getTime()) / 6e4;
  if (B > k) if (Q) $.push(`Large Fajr-sunrise gap (${Math.round(B)}min) at high latitude - normal in winter`);
  else $.push("Fajr to sunrise gap is unusually large");
  let _ = (X.isha.getTime() - X.maghrib.getTime()) / 6e4;
  if (_ > G) if (Q) $.push(`Large Maghrib-Isha gap (${Math.round(_)}min) at high latitude - normal in winter`);
  else $.push("Maghrib to Isha gap is unusually large");
  if (W) {
    let q = (X.maghrib.getTime() - X.sunrise.getTime()) / 36e5;
    if (q < 2 || q > 22) $.push(`Extreme day length (${q.toFixed(1)}h) at latitude ${J.latitude.toFixed(1)}°`);
  } else if (K > 60) {
    let q = (X.maghrib.getTime() - X.sunrise.getTime()) / 36e5;
    if (q < 4 || q > 20) $.push("Day length is unusual for high latitude");
  }
  return { isValid: $.filter((q) => !q.includes("at high latitude") && !q.includes("at latitude") && !q.includes("Extreme day length")).length === 0, issues: $ };
}
function X0(X, Z) {
  return { calculationTime: Date.now() - Z, iterationsUsed: X.iterations, convergenceAchieved: X.converged, maxError: X.error ?? 0, precision: X.error ? 1 / X.error : 1 / 0 };
}
function $Z(X, Z = {}) {
  let $ = Math.abs(X.latitude), J = { ...rZ, ...Z };
  if ($ > 60) J.maxIterations = Math.max(J.maxIterations, 5), J.convergenceThreshold = Math.min(J.convergenceThreshold, 15);
  else if ($ > 45) J.maxIterations = Math.max(J.maxIterations, 4), J.convergenceThreshold = Math.min(J.convergenceThreshold, 20);
  if ($ < 23.5) J.enableHighLatitudeAdjustment = false;
  return J;
}
function Z0(X, Z) {
  let $ = 100;
  if (!X.converged) $ -= 20;
  if (X.error && X.error > 60) $ -= Math.min(30, X.error / 2);
  if (Math.abs(Z.coordinates.latitude) > 60 && !Z.precision.enableHighLatitudeAdjustment) $ -= 25;
  if (X.iterations >= Z.precision.maxIterations) $ -= 10;
  return Math.max(0, Math.min(100, $));
}
function VX(X, Z, $, J = {}) {
  let K = $Z(Z, J.precisionConfig);
  return { date: X, coordinates: Z, methodParams: $, methodCode: J.methodCode, elevation: J.elevation ?? 0, highLatitudeRule: J.highLatitudeRule ?? "NightMiddle", asrSchool: J.asrSchool ?? "Standard", iterations: J.iterations ?? 1, precision: K };
}
function $0(X, Z) {
  let $ = /* @__PURE__ */ new Map();
  for (let J of X) {
    let K = { ...Z, date: J }, Q = FX(K), W = J.toISOString().split("T")[0];
    $.set(W, Q);
  }
  return $;
}
var XX = { validateResults: eZ, calculateMetrics: X0, createAdaptiveConfig: $Z, calculateConfidence: Z0, createContext: VX, batchCalculate: $0 };
function QZ(X, Z) {
  let $ = h(X.getFullYear(), X.getMonth() + 1, X.getDate(), 0), J = n($), K = g(J), Q = i(J), W = _X(J, Q), k = sX(K, W), G = rX(k, J), B = m(J), _ = t(J, B), Y = T(I(_) * I(G)), q = kX(w(_) * I(G), w(G)), H = oX(J);
  return { declination: Y, rightAscension: pX(q), equationOfTime: H, julianDay: $ };
}
function KZ(X, Z) {
  let J = 12 - Z.longitude / 15;
  while (J < 0) J += 24;
  while (J >= 24) J -= 24;
  return J;
}
function J0(X, Z, $, J = 0) {
  let K = QZ(X, Z), { declination: Q } = K, { latitude: W } = Z, k = K0(J), G = $ + k, B = b(W), _ = b(Q), Y = b(G), q = (Math.sin(Y) - Math.sin(B) * Math.sin(_)) / (Math.cos(B) * Math.cos(_));
  if (Math.abs(q) > 1) return { angle: NaN, isValid: false, reason: q > 1 ? "Sun never reaches specified angle (too low)" : "Sun never drops to specified angle (midnight sun)" };
  return { angle: vX(q), isValid: true };
}
function Q0(X, Z, $ = -0.833, J = 0) {
  let K = KZ(X, Z), Q = J0(X, Z, $, J);
  if (!Q.isValid) return { sunrise: NaN, sunset: NaN, isValid: false };
  let k = Q.angle / 15;
  return { sunrise: JZ(K - k), sunset: JZ(K + k), isValid: true };
}
function K0(X) {
  if (X <= 0) return 0;
  return 0.0347 * Math.sqrt(X);
}
function JZ(X) {
  while (X < 0) X += 24;
  while (X >= 24) X -= 24;
  return X;
}
function IX(X, Z, $) {
  let J = QZ(X, Z), { declination: K } = J, { latitude: Q, longitude: W } = Z, k = ($ - 12) * 15 - W, G = b(Q), B = b(K), _ = b(k);
  return jX(T(I(G) * I(B) + w(G) * w(B) * w(_)));
}
function W0(X, Z) {
  let $ = KZ(X, Z), J = IX(X, Z, $);
  for (let K = 0; K < 5; K++) {
    let W = $ - 1e-3, k = $ + 1e-3, G = IX(X, Z, W), B = IX(X, Z, k);
    if (G > J) $ = W, J = G;
    else if (B > J) $ = k, J = B;
    else break;
  }
  return { time: $, elevation: J };
}
function WZ(X, Z) {
  if (!Q0(X, Z).isValid) if (W0(X, Z).elevation > -0.833) return { isPolarDay: true, isPolarNight: false, reason: "Sun never sets (polar day)" };
  else return { isPolarDay: false, isPolarNight: true, reason: "Sun never rises (polar night)" };
  return { isPolarDay: false, isPolarNight: false };
}
var k0 = { validateInput: true, applyAdjustments: true, handlePolarRegions: true, includeMetadata: false };
function R(X, Z, $ = {}) {
  let J = { ...k0, ...$ }, K = Z ?? /* @__PURE__ */ new Date(), Q = [];
  try {
    if (J.validateInput) {
      let x = L(X);
      if (!x.isValid) throw Error(`Configuration validation failed: ${x.errors.map((JX) => JX.message).join(", ")}`);
    }
    let W = fX(X), k = P(W.location), G = r(W.method), B = I0(G, W), _ = VX(K, k, B, { methodCode: W.method, elevation: W.elevation ?? 0, highLatitudeRule: W.highLatitudeRule ?? "NightMiddle", iterations: W.iterations ?? 1 }), Y = WZ(K, k);
    if (Y.isPolarDay || Y.isPolarNight) if (J.handlePolarRegions) Q.push(`Polar region detected: ${Y.reason}`);
    else throw Error(`Cannot calculate prayer times in polar regions: ${Y.reason}`);
    let q = Date.now(), H = FX(_);
    if (!H.converged) Q.push("Calculation did not fully converge, results may have reduced accuracy");
    let U = H.value;
    if (J.handlePolarRegions && a(k.latitude)) U = G0(U, _, W);
    if (W.asrSchool === "Hanafi") U.asr = B0(U, k, K, W.elevation ?? 0);
    if (J.applyAdjustments && W.adjustments) U = _0(U, W.adjustments);
    if (W.timezone) U = U0(U, W.timezone);
    let M = XX.validateResults(U, _);
    if (!M.isValid) Q.push(...M.issues);
    let V = { times: U, isValid: M.isValid, warnings: Q };
    if (J.includeMetadata) {
      let x = Date.now() - q;
      V.metadata = { method: W.method, coordinates: k, iterations: H.iterations, precision: H.error ?? 0, calculationTime: x };
    }
    return V;
  } catch (W) {
    return { times: Y0(K), isValid: false, warnings: [`Calculation failed: ${W instanceof Error ? W.message : String(W)}`] };
  }
}
function NX(X, Z) {
  return { ...R(X, Z, { includeMetadata: true }).times, meta: { method: X.method, methodParams: r(X.method), coordinates: P(X.location), timezone: X.timezone, date: Z ?? /* @__PURE__ */ new Date(), julianDay: 0, highLatitudeAdjustment: X.highLatitudeRule, adjustments: X.adjustments } };
}
function G0(X, Z, $) {
  let { coordinates: J, date: K, methodParams: Q } = Z, W = $.highLatitudeRule ?? "NightMiddle", k = tX({ fajr: X.fajr, sunrise: X.sunrise, sunset: X.sunset, isha: X.isha, maghrib: X.maghrib }, J, K, { fajr: Q.fajr, isha: typeof Q.isha === "number" ? Q.isha : void 0, maghrib: typeof Q.maghrib === "number" ? Q.maghrib : void 0 }, W);
  return { ...X, fajr: k.fajr, isha: k.isha, maghrib: k.maghrib };
}
function B0(X, Z, $, J) {
  let Q = new j($, Z).afternoon(2), W = new Date($), k = Math.floor(Q), G = Q % 1 * 60, B = G % 1 * 60;
  return W.setUTCHours(k, Math.floor(G), Math.floor(B), 0), W;
}
function _0(X, Z) {
  let $ = { ...X };
  for (let [J, K] of Object.entries(Z)) if (J in $ && typeof K === "number") {
    let Q = J;
    if (Q !== "sunrise") $[Q] = E($[Q], K);
  }
  return $;
}
function U0(X, Z) {
  return { fajr: v(X.fajr, Z), sunrise: v(X.sunrise, Z), dhuhr: v(X.dhuhr, Z), asr: v(X.asr, Z), sunset: v(X.sunset, Z), maghrib: v(X.maghrib, Z), isha: v(X.isha, Z) };
}
function Y0(X) {
  let Z = new Date(X);
  return Z.setHours(0, 0, 0, 0), { fajr: E(Z, 300), sunrise: E(Z, 360), dhuhr: E(Z, 720), asr: E(Z, 900), sunset: E(Z, 1080), maghrib: E(Z, 1085), isha: E(Z, 1170) };
}
function HX(X, Z, $) {
  let J = [], K = new Date(Z, $, 0).getDate();
  for (let Q = 1; Q <= K; Q++) {
    let W = new Date(Z, $ - 1, Q), k = R(X, W);
    J.push({ date: W, times: k.times, isValid: k.isValid });
  }
  return J;
}
function wX(X, Z, $) {
  let J = [], K = new Date(Z);
  while (K <= $) {
    let Q = R(X, new Date(K));
    J.push({ date: new Date(K), times: Q.times, isValid: Q.isValid }), K.setDate(K.getDate() + 1);
  }
  return J;
}
function EX(X, Z) {
  let $ = Z ?? /* @__PURE__ */ new Date(), J = ["fajr", "sunrise", "dhuhr", "asr", "maghrib", "isha"], K = null, Q = null, W;
  for (let k = 0; k < J.length; k++) {
    let G = J[k], B = X[G];
    if ($ < B) {
      if (Q = G, W = Math.round((B.getTime() - $.getTime()) / 6e4), k > 0) K = J[k - 1];
      break;
    }
  }
  if (!Q) Q = "fajr", K = "isha";
  return { current: K, next: Q, timeUntilNext: W };
}
function q0(X) {
  return L(X);
}
function F0(X, Z) {
  let $ = { reducedPrecision: false, skipValidation: false, batchSize: 30, ...Z };
  return { ...X, iterations: $.reducedPrecision ? 1 : X.iterations ?? 3 };
}
function V0(X, Z) {
  let $ = Date.now(), J = R(X, Z, { includeMetadata: true }), Q = { totalTime: Date.now() - $, ...J.metadata };
  return { ...J, perfMetrics: Q };
}
function I0(X, Z) {
  let $ = { ...X };
  if (Z.fajr !== void 0) $.fajr = Z.fajr;
  if (Z.isha !== void 0) $.isha = Z.isha;
  if (Z.maghrib !== void 0) $.maghrib = Z.maghrib;
  if (Z.midnight !== void 0) $.midnight = Z.midnight;
  if (Z.shafaq !== void 0) $.shafaq = Z.shafaq;
  return $;
}
var zX = { validateConfig: q0, createBulkConfig: F0, getCurrentPrayer: EX, calculateMonthly: HX, calculateRange: wX, withMetrics: V0 };
function ZX(X, Z = /* @__PURE__ */ new Date()) {
  let $ = R(X, Z);
  if (!$.isValid) throw Error(`Cannot calculate Sunnah times: ${$.warnings.join(", ")}`);
  let J = $.times, K = new Date(Z);
  K.setDate(K.getDate() + 1);
  let Q = R(X, K);
  if (!Q.isValid) throw Error(`Cannot calculate tomorrow's Fajr: ${Q.warnings.join(", ")}`);
  let W = Q.times.fajr, k = W.getTime() - J.maghrib.getTime(), G = k / 6e4, B = k / 3, _ = k / 2, Y = k * 0.6666666666666666, q = new Date(J.maghrib.getTime() + B), H = new Date(J.maghrib.getTime() + _), U = new Date(J.maghrib.getTime() + Y), M = new Date(J.sunrise.getTime() + 9e5), V = new Date(J.dhuhr.getTime() - 9e5);
  return { middleOfNight: c(H), lastThirdOfNight: c(U), firstThirdOfNight: c(q), duhaStart: c(M), duhaEnd: c(V), meta: { nightDuration: Math.round(G), calculation: { maghribTime: J.maghrib.getTime(), nextFajrTime: W.getTime(), sunriseTime: J.sunrise.getTime(), dhuhrTime: J.dhuhr.getTime() } } };
}
function c(X) {
  let Z = new Date(X);
  if (Z.setSeconds(0, 0), X.getSeconds() >= 30) Z.setMinutes(Z.getMinutes() + 1);
  return Z;
}
var l = class {
  config;
  constructor(X) {
    this.config = X;
  }
  calculate(X = /* @__PURE__ */ new Date()) {
    return ZX(this.config, X);
  }
  getNightThirds(X = /* @__PURE__ */ new Date()) {
    let Z = this.calculate(X);
    return { first: Z.firstThirdOfNight, middle: Z.middleOfNight, last: Z.lastThirdOfNight, nightDuration: Z.meta.nightDuration };
  }
  getDuhaWindow(X = /* @__PURE__ */ new Date()) {
    let Z = this.calculate(X), $ = Z.duhaEnd.getTime() - Z.duhaStart.getTime();
    return { start: Z.duhaStart, end: Z.duhaEnd, duration: Math.round($ / 6e4) };
  }
  isCurrentlyInPeriod(X, Z = /* @__PURE__ */ new Date()) {
    let $ = this.calculate(Z), J = /* @__PURE__ */ new Date();
    switch (X) {
      case "duha":
        return J >= $.duhaStart && J <= $.duhaEnd;
      case "lastThird":
        let K = new Date(Z);
        K.setDate(K.getDate() + 1);
        let Q = R(this.config, K);
        if (!Q.isValid) return false;
        return J >= $.lastThirdOfNight && J <= Q.times.fajr;
      case "firstThird":
        return J >= $.firstThirdOfNight && J <= $.middleOfNight;
      case "middleNight":
        let W = 18e5;
        return Math.abs(J.getTime() - $.middleOfNight.getTime()) <= W;
      default:
        return false;
    }
  }
};
function N0(X) {
  return new l(X);
}
var H0 = { saudi_arabia: { bounds: { north: 32.5, south: 16, east: 50.5, west: 34.5 }, method: "UmmAlQura" }, qatar: { bounds: { north: 26.2, south: 24.5, east: 51.7, west: 50.7 }, method: "Qatar" }, uae: { bounds: { north: 26.5, south: 22.5, east: 56.5, west: 51 }, method: "Dubai" }, kelantan_malaysia: { bounds: { north: 6.5, south: 4.5, east: 102.5, west: 101 }, method: "JAKIMKN" }, malaysia: { bounds: { north: 7.5, south: 0.5, east: 119.5, west: 99.5 }, method: "JAKIM" }, indonesia: { bounds: { north: 6, south: -11, east: 141, west: 95 }, method: "Kemenag" }, singapore: { bounds: { north: 1.5, south: 1.2, east: 104.1, west: 103.6 }, method: "Singapore" }, france: { bounds: { north: 51.5, south: 41.5, east: 9.5, west: -5 }, method: "France15" }, turkey: { bounds: { north: 42.5, south: 35.5, east: 45, west: 25.5 }, method: "Turkey" }, russia: { bounds: { north: 82, south: 41.5, east: -169, west: 19 }, method: "Russia" }, iran: { bounds: { north: 40, south: 25, east: 63.5, west: 44 }, method: "Tehran" } };
var GZ = { north_america: { primary: "ISNA", alternatives: ["MWL"], reason: "ISNA is widely accepted across North American Muslim communities" }, europe: { primary: "MWL", alternatives: ["France15", "France12", "France18", "Turkey", "Russia"], reason: "Muslim World League method is internationally recognized" }, middle_east: { primary: "UmmAlQura", alternatives: ["MWL", "Egypt"], reason: "Umm Al-Qura is used in the holy cities and widely accepted in the region" }, southeast_asia: { primary: "JAKIM", alternatives: ["JAKIMKN", "Kemenag", "Singapore", "MWL"], reason: "JAKIM method is optimized for tropical latitudes" }, africa: { primary: "Egypt", alternatives: ["MWL"], reason: "Egyptian method is traditional in North Africa" }, south_asia: { primary: "MWL", alternatives: ["Kemenag"], reason: "Muslim World League method is widely used in the Indian subcontinent" }, unknown: { primary: "MWL", alternatives: ["ISNA", "UmmAlQura"], reason: "Muslim World League is a safe default for unknown regions" } };
function RX(X) {
  for (let [$, J] of Object.entries(H0)) if (yX(X, J.bounds)) {
    let K = kZ(X);
    return { recommended: J.method, region: K.region, alternatives: [K.primary, ...K.alternatives.slice(0, 2)], reason: `Specific method for ${$.replace("_", " ")}: ${J.method}` };
  }
  let Z = kZ(X);
  return { recommended: Z.primary, region: Z.region, alternatives: Z.alternatives, reason: Z.reason };
}
function kZ(X) {
  let Z = BX(X);
  return { ...GZ[Z], region: Z };
}
function $X(X) {
  let Z = BX(X), $ = GZ[Z];
  return [$.primary, ...$.alternatives];
}
function DX(X, Z) {
  if ($X(Z).includes(X)) return { suitable: true };
  let J = Math.abs(Z.latitude);
  if (J > 55) {
    if (["Russia", "MWL", "Moonsighting"].includes(X)) return { suitable: true, reason: "Method adapted for high latitudes" };
    return { suitable: false, reason: "Method may not work well at high latitudes. Consider Russia or Moonsighting method." };
  }
  if (J < 25) {
    if (["JAKIM", "JAKIMKN", "Kemenag", "Singapore", "MWL"].includes(X)) return { suitable: true, reason: "Method suitable for tropical regions" };
    return { suitable: false, reason: "Method may not be optimal for tropical regions. Consider JAKIM, JAKIMKN, or Kemenag." };
  }
  return { suitable: true, reason: "Method will work but may not be regionally optimal" };
}
var p = class {
  config;
  constructor(X) {
    let Z = L(X);
    if (!Z.isValid) {
      let $ = Z.errors.map((J) => `${J.field}: ${J.message}`).join(`
  `);
      throw Error(`Prayer time configuration validation failed:
  ${$}

Please check your configuration and ensure all required fields are properly set.`);
    }
    this.config = { ...X };
  }
  static calculate(X, Z) {
    return OX(X, Z);
  }
  static calculateSunnah(X, Z) {
    return MX(X, Z);
  }
  calculate(X) {
    try {
      let Z = this.parseDate(X), $ = R(this.config, Z);
      if (!$.isValid) {
        let J = $.warnings.join("; ");
        throw Error(`Prayer time calculation failed: ${J}

This may occur due to:
  • Extreme latitude locations during certain seasons
  • Invalid date or timezone
  • Calculation method incompatible with location

Try using a different high latitude rule or adjust your location/method.`);
      }
      return $.times;
    } catch (Z) {
      if (Z instanceof Error) {
        if (Z.message.includes("Prayer time calculation failed")) throw Z;
        throw Error(`Failed to calculate prayer times: ${Z.message}

Please verify your configuration and try again.`);
      }
      throw Error("An unexpected error occurred during prayer time calculation. Please check your input parameters and try again.");
    }
  }
  calculateWithMeta(X) {
    let Z = this.parseDate(X);
    return NX(this.config, Z);
  }
  calculateFormatted(X, Z = "24h") {
    let $ = this.calculate(X);
    return qX($, Z, this.config.rounding);
  }
  calculateForMonth(X, Z) {
    let $ = HX(this.config, X, Z);
    return { year: X, month: Z, times: $.map((J) => J.times) };
  }
  calculateForRange(X) {
    let Z = wX(this.config, X.start, X.end);
    return { range: X, times: Z.map(($) => $.times) };
  }
  getCurrentPrayer(X) {
    let Z = this.parseDate(X), $ = this.calculate(Z);
    return EX($, Z);
  }
  getNextPrayer(X) {
    let Z = this.getCurrentPrayer(X);
    if (!Z.next || Z.timeUntilNext === void 0) return null;
    let $ = this.parseDate(X), K = this.calculate($)[Z.next];
    return { prayer: Z.next, time: K, minutesUntil: Z.timeUntilNext };
  }
  updateConfig(X) {
    let Z = { ...this.config, ...X }, $ = L(Z);
    if (!$.isValid) throw Error(`Invalid configuration update: ${$.errors.map((J) => J.message).join(", ")}`);
    this.config = Z;
  }
  getConfig() {
    return { ...this.config };
  }
  validateConfiguration() {
    return L(this.config);
  }
  isMethodSuitable() {
    let X = P(this.config.location);
    return DX(this.config.method, X);
  }
  getAlternativeMethods() {
    let X = P(this.config.location);
    return $X(X);
  }
  getSunnahTimes(X) {
    try {
      let Z = this.parseDate(X);
      return ZX(this.config, Z);
    } catch (Z) {
      if (Z instanceof Error) throw Error(`Failed to calculate Sunnah times: ${Z.message}

Sunnah times calculation requires valid prayer times for the current and next day. Please ensure your location and method configuration is correct.`);
      throw Error("An unexpected error occurred while calculating Sunnah times. Please verify your configuration and try again.");
    }
  }
  getNightThirds(X) {
    let Z = this.getSunnahTimes(X);
    return { first: Z.firstThirdOfNight, middle: Z.middleOfNight, last: Z.lastThirdOfNight, nightDuration: Z.meta.nightDuration };
  }
  getDuhaWindow(X) {
    let Z = this.getSunnahTimes(X), $ = Z.duhaEnd.getTime() - Z.duhaStart.getTime();
    return { start: Z.duhaStart, end: Z.duhaEnd, duration: Math.round($ / 6e4) };
  }
  isCurrentlyInSunnahPeriod(X, Z) {
    let $ = new l(this.config), J = this.parseDate(Z);
    return $.isCurrentlyInPeriod(X, J);
  }
  parseDate(X) {
    if (!X) return /* @__PURE__ */ new Date();
    if (X instanceof Date) {
      if (isNaN(X.getTime())) throw Error("Invalid Date object provided. Please ensure the date is properly constructed.");
      return X;
    }
    if (typeof X === "string") {
      let Z = new Date(X);
      if (isNaN(Z.getTime())) throw Error(`Invalid date string: "${X}". Please use a valid date format like "2024-01-15" or "2024-01-15T12:00:00Z".`);
      return Z;
    }
    if (typeof X === "number") {
      let Z = new Date(X).getFullYear();
      if (Z < 1900 || Z > 2200) throw Error(`Date timestamp appears to be invalid (year: ${Z}). Please provide a timestamp in milliseconds since Unix epoch.`);
      let $ = new Date(X);
      if (isNaN($.getTime())) throw Error(`Invalid timestamp: ${X}. Please provide a valid timestamp in milliseconds.`);
      return $;
    }
    throw Error(`Invalid date input type. Expected Date object, string, number, or undefined. Received: ${typeof X}`);
  }
};
function OX(X, Z) {
  return new p(X).calculate(Z);
}
function BZ(X) {
  let Z = P(X);
  return RX(Z);
}
function MX(X, Z) {
  let $ = Z ? Z instanceof Date ? Z : new Date(Z) : /* @__PURE__ */ new Date();
  return ZX(X, $);
}
function _Z(X) {
  return L(X);
}
function UZ(X, Z = "24h") {
  return qX(X, Z);
}
function YZ(X, Z) {
  let $ = zX.createBulkConfig(X, Z);
  return new p($);
}
var O0 = p;
var w0 = { calculate: OX, suggestMethod: BZ, validate: _Z, format: UZ, createBulkCalculator: YZ, calculateSunnah: MX };
function E0(X) {
  if (!X || typeof X !== "object" || X === null) return false;
  let Z = X;
  return "fajr" in Z && Z.fajr instanceof Date && "sunrise" in Z && Z.sunrise instanceof Date && "dhuhr" in Z && Z.dhuhr instanceof Date && "asr" in Z && Z.asr instanceof Date && "sunset" in Z && Z.sunset instanceof Date && "maghrib" in Z && Z.maghrib instanceof Date && "isha" in Z && Z.isha instanceof Date;
}
function z0(X) {
  if (!X || typeof X !== "object" || X === null) return false;
  let Z = X;
  if (!("method" in Z) || typeof Z.method !== "string") return false;
  if (!("location" in Z)) return false;
  if (Array.isArray(Z.location)) return true;
  if (Z.location && typeof Z.location === "object" && Z.location !== null && "latitude" in Z.location && typeof Z.location.latitude === "number") return true;
  return false;
}
var R0 = ["fajr", "sunrise", "dhuhr", "asr", "sunset", "maghrib", "isha"];
var D0 = ["24h", "12h", "12hNS", "timestamp", "iso8601"];
var M0 = "1.0.0";
var P0 = "prayer-times";
var f6 = { name: P0, version: M0, description: "TypeScript prayer times calculation library inspired by adhan.js", author: "Prayer Times Library Team", algorithms: "Astronomical calculations similar to adhan.js", precision: "Validated against reference implementations" };
var r6 = p;
export {
  s as CALCULATION_METHODS,
  O0 as Calculator,
  zX as CalculatorUtils,
  P0 as LIBRARY_NAME,
  o as MECCA_COORDINATES,
  f6 as METADATA,
  SX as METHOD_DESCRIPTIONS,
  LX as METHOD_NAMES,
  R0 as PRAYER_NAMES,
  p as PrayerTimeCalculator,
  w0 as PrayerTimeUtils,
  XX as PrecisionUtils,
  l as SunnahTimesCalculator,
  D0 as TIME_FORMATS,
  M0 as VERSION,
  R as calculateEngine,
  OX as calculatePrayerTimes,
  NX as calculatePrayerTimesWithMeta,
  R as calculatePrecise,
  MX as calculateSunnahTimes,
  YZ as createBulkCalculator,
  N0 as createSunnahCalculator,
  r6 as default,
  UZ as formatPrayerTimesToString,
  OZ as getAllMethods,
  vZ as getDistanceToMecca,
  DZ as getMethodDescription,
  RZ as getMethodName,
  r as getMethodParams,
  bZ as getQiblaDirection,
  $X as getRegionalMethods,
  DX as isMethodSuitableForLocation,
  z0 as isPrayerTimeConfig,
  E0 as isPrayerTimes,
  QX as isValidMethod,
  RX as suggestMethod,
  BZ as suggestMethodForLocation,
  _Z as validatePrayerTimeConfig
};
//# sourceMappingURL=@masaajid_prayer-times.js.map
