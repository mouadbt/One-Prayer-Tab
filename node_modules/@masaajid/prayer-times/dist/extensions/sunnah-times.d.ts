import type { PrayerTimeConfig } from "../types";
export interface SunnahTimes {
    middleOfNight: Date;
    lastThirdOfNight: Date;
    firstThirdOfNight: Date;
    duhaStart: Date;
    duhaEnd: Date;
    meta: {
        nightDuration: number;
        calculation: {
            maghribTime: number;
            nextFajrTime: number;
            sunriseTime: number;
            dhuhrTime: number;
        };
    };
}
export declare function calculateSunnahTimes(config: PrayerTimeConfig, date?: Date): SunnahTimes;
export declare class SunnahTimesCalculator {
    private config;
    constructor(config: PrayerTimeConfig);
    calculate(date?: Date): SunnahTimes;
    getNightThirds(date?: Date): {
        first: Date;
        middle: Date;
        last: Date;
        nightDuration: number;
    };
    getDuhaWindow(date?: Date): {
        start: Date;
        end: Date;
        duration: number;
    };
    isCurrentlyInPeriod(period: "duha" | "lastThird" | "firstThird" | "middleNight", date?: Date): boolean;
}
export declare function createSunnahCalculator(config: PrayerTimeConfig): SunnahTimesCalculator;
//# sourceMappingURL=sunnah-times.d.ts.map